# MLIR 关键概念速览（占位）

后续建议按“能跑通 + 能写 pass”为目标，逐条补齐并配套最小示例：

- Dialect / Operation / Type / Attribute
- SSA / Region / Block / Value
- Verifier 与诊断
- PatternRewriter / DRR
- PassManager 与 pass pipeline
- Dialect Conversion（TypeConverter / ConversionTarget）
- Bufferization（tensor → memref）
- `linalg` / `scf` / `affine` / `vector` 的定位与常用优化入口

