# ACompiler Dialect (header plan)

Planned headers:

- `ACompilerDialect.h`
- `ACompilerOps.h`
- `ACompilerTypes.h`
- `ACompilerAttrs.h`

These files will be introduced when MLIR dialect implementation starts.
